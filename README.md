# Strix

* Clone repo.
* Use data.sql to insert sample data to db.
* Install dependencies
```bash
composer install
```
* Run project 
```bash
php bin/console server:run
```
* Run tests
```bash
php bin/phpunit
```